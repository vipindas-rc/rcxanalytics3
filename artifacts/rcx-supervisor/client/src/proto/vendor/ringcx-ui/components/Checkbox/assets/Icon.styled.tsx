import { createStyles } from '@material-ui/core/styles';

export const IconTheme = createStyles({
    root: {
        fontSize: 21,
    },
    fontSizeSmall: {
        fontSize: 21,
    },
    fontSizeLarge: {
        fontSize: 24,
    },
});
