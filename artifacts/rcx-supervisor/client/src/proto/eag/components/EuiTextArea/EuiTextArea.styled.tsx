import styled from 'styled-components';

export const StyledParagraph = styled.p`
    margin: 0;
`;
export const StyledLoadingTextSpan = styled.span`
    color: ${({ theme }) => theme.colors.gray[700]};
`;
