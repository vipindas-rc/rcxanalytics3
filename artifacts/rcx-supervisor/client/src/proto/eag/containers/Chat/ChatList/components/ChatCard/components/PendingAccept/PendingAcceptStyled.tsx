import { Button } from '@ringcx/ui';
import styled from 'styled-components';

// eslint-disable-next-line @typescript-eslint/no-explicit-any
export const PendingButton = styled(Button as any)`
    max-width: 75px;
    text-align: center;
    white-space: nowrap;
    overflow: hidden;
`;
