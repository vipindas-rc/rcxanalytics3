import { ExternalLinkIcon } from '@ringcx/ui';
import styled from 'styled-components';

export const StyledExternalLinkIcon = styled(ExternalLinkIcon)`
    font-size: 12px;
    color: ${({ theme }) => theme.colors.gray[800]};
`;
