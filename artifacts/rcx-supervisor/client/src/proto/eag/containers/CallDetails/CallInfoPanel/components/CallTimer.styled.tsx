import styled from 'styled-components';

export const CallIconStyled = styled.span`
    padding-right: 4px;
`;

export const CallTime = styled.span`
    font-size: 14px;
    font-weight: 500;
    letter-spacing: 0.25px;
`;
