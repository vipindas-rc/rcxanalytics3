import { defineConfig, devices } from '@playwright/test'
export default defineConfig({ testDir: './tests/gateway', reporter: 'list', retries: 0, use: { baseURL: process.env.PLAYWRIGHT_BASE_URL ?? 'http://127.0.0.1:5019', trace: 'retain-on-failure', screenshot: 'only-on-failure' }, projects: [{ name: 'chrome', use: { ...devices['Desktop Chrome'], channel: 'chrome' } }] })