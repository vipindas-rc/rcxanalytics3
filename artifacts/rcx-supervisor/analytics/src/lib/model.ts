export type Renderer = 'echarts' | 'chartjs' | 'plotly'
export type Row = Record<string, string | number>
export type Field = { id: string; name: string; type: 'category' | 'date' | 'number'; unit?: string; values?: string[]; min?: number; max?: number; precision?: number; numerator?: string; denominator?: string }
export type Dataset = { id: string; title: string; seed: number; fields: Field[]; rows: Row[]; createdAt: string; colors: Record<string, number> }
export type Filter = { field: string; operator: 'eq' | 'in' | 'gte' | 'lte'; value: string | number | Array<string | number> }
/** Periods are half-open [from, to); timestamps are ISO strings. */
export type AnalysisScope = { period: { from: string; to: string }; timezone: string; filters: Filter[] }
export type AnalysisReference = { datasetId: string; datasetRevision: string; reportId?: string; reportVersion?: number; metricDefinitionVersion: number; scope: AnalysisScope; queryId: string }
export type EvidenceMetric = { id: string; version: number; name: string; formula: string; unit: string; grain: string; exclusions: string[]; denominator?: string; assumption?: string }
export type EvidenceFact = { metricId: string; value: number | null; numerator?: number; denominator?: number; comparisonValue?: number | null; difference?: number | null; label?: string }
export type AnalysisEvidence = { id: string; reference: AnalysisReference; metrics: EvidenceMetric[]; facts: EvidenceFact[]; groups: Row[]; provenance: { synthetic: true; seed: number; generatorVersion: string; generatedAt: string }; limitations: string[] }
/** A page of source records, distinct from the grouped rows used by a chart. */
export type SourceRecordsPage = { rows: Row[]; fields: Field[]; offset: number; limit: number; total: number; reference: AnalysisReference }
export type InspectionMetadata = { kind: 'source-records' | 'grouped-results'; total: number; offset: number; limit: number; reference?: AnalysisReference }
export type ChartView = { chartType: string; x?: string; y?: string; y2?: string; series?: string; aggregation: 'sum' | 'mean' | 'count' | 'ratio'; numerator?: string; denominator?: string; filters: Filter[]; sort?: 'ascending' | 'descending'; orientation?: 'horizontal' | 'vertical'; target?: number; labelField?: string }
export type Kpi = { label: string; value: number; unit?: string; detail?: string }
export type Artifact = { id: string; title: string; datasetId: string; view: ChartView; renderer: Renderer; createdAt: string; summary: string; kpis: Kpi[]; notice?: string; analysisReference?: AnalysisReference; evidence?: AnalysisEvidence }
export type Capability = { renderer: Renderer; supported: boolean; reason?: string }
export type Presentation = { artifactId: string; renderer: Renderer; spec: any; rows: Row[]; capabilities: Capability[]; notice?: string; fields?: Field[]; kpis?: Kpi[]; evidence?: AnalysisEvidence; inspection?: InspectionMetadata }
export type SourceContext = { artifactId: string; filters: Filter[]; sourceLabel?: string; dashboardId?: string; dashboardRevision?: number; briefingId?: string; briefingWidgetId?: string; reportingWindow?: string; comparisonWindow?: string; metricTitle?: string; entity?: string; datasetId?: string; datasetVersion?: string; analysisReference?: AnalysisReference }
export type ReportContext = { reportId: string; reportVersion: number; title: string; type: 'dashboard' | 'report'; availability: 'supported' | 'preview'; datasetId?: string; datasetVersion?: string; sourceUrl: string; analysisReference?: AnalysisReference; selectedContentIds?: string[]; presentationPreference?: 'auto' | 'chart' | 'table' }
export type Message = { id: string; role: 'user' | 'assistant'; text: string; createdAt: string; artifactIds?: string[]; suggestions?: string[]; choices?: string[]; requestId?: string; dashboardId?: string; dashboardRevision?: number; sourceContext?: SourceContext; reportContext?: ReportContext; selectedFollowUpFrom?: string; analysisReference?: AnalysisReference; evidence?: AnalysisEvidence }
export type Session = { id: string; title: string; renamed?: boolean; advisor?: boolean; advisorContext?: SourceContext; reportContext?: ReportContext; projectId: string | null; createdAt: string; updatedAt: string; messages: Message[] }
export type Project = { id: string; name: string }
export type SavedChart = { id: string; artifactId: string; projectId: string; title: string }
export type DashboardWidget = { id: string; artifactId: string; title: string }
export type DashboardRevision = { revision: number; title: string; widgets: DashboardWidget[]; filters: Filter[]; createdAt: string }
export type Dashboard = { id: string; projectId: string; title: string; revision: number; widgets: DashboardWidget[]; filters: Filter[]; history: DashboardRevision[] }
export type BriefingScenario = 'morning' | 'midday'
export type BriefingWidget = { id: string; section: 'Needs attention' | 'Team & capacity' | 'AI & customer outcomes' | 'Chart examples'; artifactId: string; metricTitle?: string; entity?: string; finding: string; value: string; comparison: string; severity: 'critical' | 'warning' | 'watch' | 'healthy'; explanation: string; definition: string; source: 'Catalog' | 'Synthetic extension'; sourceUrl?: string; priority?: boolean; investigatePrompt: string }
export type BriefingSnapshot = { id: string; scenario: BriefingScenario; title: string; reportingWindow: string; comparisonWindow: string; summary: string[]; createdAt: string; widgets: BriefingWidget[] }
export type TurnRequest = { id: string; sessionId: string; question: string; status: 'pending' | 'completed' | 'failed'; phase: string; /** Actual controller stages, retained so a reconnecting client can resume the live status. */ phaseHistory?: Array<{ phase: string; at: string }>; error?: string; artifactIds?: string[]; userMessageId: string; createdAt: string; /** Captured once at intake; retries retain the original analytical instant. */ asOf?: string; timezone?: string; analysisPlan?: Record<string, unknown>; contextArtifactId?: string; contextFilters?: Filter[]; contextDashboardId?: string; sourceLabel?: string; sourceContext?: SourceContext; reportContext?: ReportContext; cached?: boolean }
export type Workspace = { version: 4; revision: number; sessions: Session[]; projects: Project[]; datasets: Dataset[]; artifacts: Artifact[]; savedCharts: SavedChart[]; dashboards: Dashboard[]; requests: TurnRequest[]; preferences: Record<string, Renderer>; briefings: BriefingSnapshot[]; responseCache?: Record<string, unknown> }
export type ConversationInput = { sessionId: string; requestId: string; question: string; timezone?: string; contextArtifactId?: string; contextFilters?: Filter[]; contextDashboardId?: string; sourceLabel?: string; sourceContext?: SourceContext; reportId?: string; reportVersion?: string | number; selectedContentIds?: string[]; presentationPreference?: 'auto' | 'chart' | 'table'; renderer?: Renderer; retryOf?: string; selectedFollowUpFrom?: string }
